<?xml version="1.0" encoding="utf-8"?>
<!--
    Coral's splash screen background.
    Just the deep-ocean gradient — the centered wave logo would crash on
    older launchers because <bitmap> can't reference <vector> drawables.
    Compose draws the actual UI on top within ~100ms anyway, so the user
    only sees this gradient for a split second.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:type="linear"
                android:angle="270"
                android:startColor="#1A2A38"
                android:endColor="#050508" />
        </shape>
    </item>
</layer-list>
