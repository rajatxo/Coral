<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base Coral theme. Tuned for FULLY TRANSPARENT system bars (the
        ViTune-style seamless edge-to-edge look).

        Key settings:
          - windowBackground: solid #121212 (matches CoralColors.Surface
            so there's no flash when Compose takes over from the splash)
          - statusBarColor + navigationBarColor: transparent
          - windowDrawsSystemBarBackgrounds: true (content draws behind bars)
          - windowLayoutInDisplayCutoutMode: shortEdges (full bleed on
            notched/punched-hole displays)
    -->
    <style name="Theme.Coral" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
</resources>
